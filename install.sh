# Update Submodules
git submodule update --init --recursive
# This script is used to update the submodules of the project.
# It uses git submodule update --init --recursive to update the submodules.
# The script first updates the submodules using git submodule update --init --recursive.
# Then it updates the submodules using git submodule update --init --recursive.
# Finally, it updates the submodules using git submodule update --init --recursive.

npm install

node ./installer.js
# This script is used to install the necessary dependencies for the project.
# It uses npm to install the dependencies listed in package.json.
# It also installs the dependencies for the backend and frontend separately.
# The script first installs the dependencies for the backend using npm install.
# Then it installs the dependencies for the frontend using npm install.
# Finally, it installs the dependencies for the frontend using npm install.
# The script also installs the dependencies for the frontend using npm install.